spring:
  datasource:
    url: jdbc:mysql://localhost:3306/person?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: wwwpsxiaoji
    username: root
  devtools:
    restart:
      additional-exclude: resources/**,static/**,templates/**


  jmx:
    enabled: false